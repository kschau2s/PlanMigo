# Alembic configuration. The database URL is injected in alembic/env.py from
# app.config.Settings (single source of truth: .env) — do not hardcode it here.

[alembic]
script_location = alembic
prepend_sys_path = .
